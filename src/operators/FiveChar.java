package operators;

/*
Распечатать 5 символов в одну строку начиная с ‘h’. Используем цикл for.
 */

public class FiveChar {
    public static void main(String[] args) {
        for (char ch = 'h'; ch <= 'l'; ch++) {
            System.out.println(ch);
        }
    }
}
